name: flutter_showcase
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.5.1 <4.0.0"
  flutter: 3.19.0 # Add this line with the exact Flutter version for CI

dependencies:
  flutter:
    sdk: flutter
  flutter_localization: ^0.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
