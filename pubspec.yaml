name: flutter_showcase
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.5.1 <4.0.0"
  flutter: 3.24.1 # Update this line with the appropriate version

dependencies:
  flutter:
    sdk: flutter
  flutter_localization: ^0.2.2

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  very_good_analysis: ^6.0.0

flutter:
  uses-material-design: true
